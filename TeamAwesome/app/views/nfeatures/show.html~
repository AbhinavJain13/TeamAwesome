%h1 Add note to selected tweet

= form_tag notes_path do

  = label :note, :name, 'Name'
  = text_field :note, 'name'

  = label :note, :notecontent, 'Note'
  = text_area :note, 'notecontent' 


  
= submit_tag 'Create Note'
